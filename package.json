{
  "name": "raml-json-enhance",
  "version": "0.4.1",
  "license": "Apache-2.0 OR CC-BY-4.0",
  "description": "Enhaces the JSON output from the RAML parser so it can be used in the ARC elements (which expect enhanced data structure)",
  "devDependencies": {
    "@babel/core": "^7.1.5",
    "@babel/preset-env": "^7.1.5",
    "babel-cli": "^6.26.0",
    "babel-plugin-minify-mangle-names": "0.4.3",
    "babel-preset-env": "^1.7.0",
    "browserify": "16.2.3",
    "datatype-expansion": "0.3.4",
    "fs-extra": "^7.0.1",
    "raml2obj": "github:advanced-rest-client/raml2obj#6.2.1",
    "uglify-js": "3.4.9"
  },
  "private": false,
  "main": [
    "raml-json-enhance.html"
  ],
  "author": "The Advanced REST client authors <arc@mulesoft.com>",
  "scripts": {
    "serve": "node_modules/.bin/polymer serve",
    "deps": "npm install --quiet && bower install --quiet",
    "update-deps": "npm update --quiet && bower update --quiet",
    "test-sauce": "node_modules/.bin/polymer test --plugin sauce --job-name \"raml-json-enhance:local-test\"",
    "test": "node_modules/.bin/polymer test --plugin local",
    "build": "node build.js"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/advanced-rest-client/raml-json-enhance.git"
  },
  "bugs": {
    "url": "https://github.com/advanced-rest-client/raml-json-enhance/issues",
    "email": "arc@mulesoft.com"
  }
}
